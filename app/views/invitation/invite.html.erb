<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= @user.name %> has invited you to collaborate</h1>
    <h3><%= @project.name %></h3>
    <h3>Please click the link below to join the team.</h3>
    <%= link_to "Confirm", "http://localhost:3000/projects/#{@project.id}" %>
  </body>
</html>